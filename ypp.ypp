%{
#include "hpp.hpp"
%}
%defines %union { Base *o; }
%token <o> ID NUM STR
%type <o> ex
%token EQ
%%
REPL : | REPL ex		{ cout << $2->dump() << endl; }
ex : ID EQ ex			{ vars[$1->val] = $3; }			// set var
ex : ID | NUM | STR
